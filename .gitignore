# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.ruff_cache/
.venv/
venv/

# Env / secrets
.env

# Data artifacts (raw pages, Parquet, DuckDB files) — never commit data.
# Anchored to the repo root so it does NOT match committed package reference
# data like src/chicago_crime_mcp/ingest/data/iucr_codes.csv.
/data/
*.parquet
*.duckdb

# OS / editor
.DS_Store
.idea/
.vscode/
